# BIEXCE Self-Test Project Rules

- Run `python -m unittest discover -s tests -v` for verification.
- Keep WIP at one task and follow `.biexce/MASTER_PLAN.md` in DAG order.
- Do not use Git, deployment tools, network services, or third-party packages.
- Edit only files listed by the active task and store evidence in `.biexce/reports/`.
