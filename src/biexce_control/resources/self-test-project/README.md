# BIEXCE Autopilot Self-Test Project

This intentionally small project contains three sequential tasks. Initialize a
working copy with `biexce fixture init --project <empty-directory>`. Reset only
an identified fixture with `biexce fixture reset --project <path>
--confirm-reset self-test-project-v1`.
