# t-001 — implement addition and subtraction

## 1. Objective
Provide pure `add` and `subtract` functions that return deterministic numeric results.

## 2. Context tối thiểu
Create `src/calculator.py`; use only the Python standard library and preserve the public function names in this task.

## 3. Acceptance criteria
- [ ] `add(2, 3)` returns `5`.
- [ ] `subtract(5, 3)` returns `2`.
Verify: `python -m unittest discover -s tests -v`

## 4. Boundaries
Owner role: bx-code
Writable files: src/calculator.py
Read-only inputs: .biexce/PROJECT_BRIEF.md, .biexce/MASTER_PLAN.md
Out-of-scope: tests, dependencies, Git, deployment, network
Depends on: none · Effort: S
