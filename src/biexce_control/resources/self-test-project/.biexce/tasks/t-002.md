# t-002 — implement multiplication and guarded division

## 1. Objective
Provide `multiply` and `divide` functions, with division by zero rejected by `ValueError`.

## 2. Context tối thiểu
Extend `src/calculator.py` after t-001; keep all calculator functions pure and dependency-free.

## 3. Acceptance criteria
- [ ] `multiply(4, 3)` returns `12`.
- [ ] `divide(8, 2)` returns `4`.
- [ ] `divide(1, 0)` raises `ValueError`.
Verify: `python -m unittest discover -s tests -v`

## 4. Boundaries
Owner role: bx-code
Writable files: src/calculator.py
Read-only inputs: .biexce/tasks/t-001.md, .biexce/MASTER_PLAN.md
Out-of-scope: tests, dependencies, Git, deployment, network
Depends on: t-001 · Effort: S
